version: "3"
services:
  auth:
    build: ./auth-api
    ports:
      - "8000:3000"
    environment:
      TOKEN_KEY: "SECURETOKENEKSAWS"
  users:
    build: ./users-api
    ports:
      - "8080:3000"
    environment:
      MONGODB_CONNECTION_URI: "mongodb+srv://ligesh_mongo_eks:WGxlVsHpOttsjDOM@cluster0.yijxija.mongodb.net/users?appName=Cluster0"
      AUTH_API_ADDRESSS: "auth:3000"
